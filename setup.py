from setuptools import find_packages, setup

setup(
    name='src',
    packages=find_packages(),
    version='0.1.0',
    description='Telegram bot aiming to notify users about the current momentum stocks among certain ETFs, based on some specific criterias.',
    author='Rafael Pacco',
    license='',
)
